'use client';
import {FormEvent,useState} from 'react';
import {ArrowLeft,ArrowRight,CheckCircle2,Mail} from 'lucide-react';
import '../login/auth.css';
export default function ForgotPassword(){const[email,setEmail]=useState('');const[sent,setSent]=useState(false);function submit(e:FormEvent){e.preventDefault();if(email)setSent(true)}return <main className="authShell"><div className="authVisual"><div className="authBrand"><span>AI</span><div><b>AI Company OS</b><small>AUTONOMOUS COMPANY</small></div></div><div className="authOrb"><div>{sent?<CheckCircle2 size={30}/>:<Mail size={30}/>}<b>{sent?'EMAIL QUEUED':'ACCOUNT RECOVERY'}</b><small>SECURE ACCESS FLOW</small></div></div><p>Password recovery is isolated from company operations. Your companies remain untouched while access is restored.</p></div><section className="authPanel"><div className="authCard"><div className="mobileBrand">AI COMPANY OS</div>{sent?<><div className="authEyebrow">CHECK YOUR INBOX</div><h1>Email sent.</h1><p className="authLead">If an account exists for <strong>{email}</strong>, a recovery link has been sent. The link is valid for a limited time.</p><button className="primaryAuth" onClick={()=>window.location.href='/login'}>Return to login <ArrowRight size={16}/></button></>:<><div className="authEyebrow">ACCOUNT RECOVERY</div><h1>Reset access.</h1><p className="authLead">Enter the email connected to your AI Company OS account.</p><form onSubmit={submit}><label>Email<div className="inputWrap"><Mail size={16}/><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></div></label><button className="primaryAuth">Send recovery email <ArrowRight size={16}/></button></form></>}<a className="backLink" href="/login"><ArrowLeft size={13}/> Back to login</a></div></section></main>}
