'use client';
import { FormEvent,useState } from 'react';
import { ArrowRight, LockKeyhole, Mail, ShieldCheck } from 'lucide-react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import './auth.css';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const router=useRouter();function submit(e:FormEvent){e.preventDefault();if(!email||!password){setError('Enter your email and password to continue.');return}localStorage.setItem('aicos-session',JSON.stringify({email,signedInAt:new Date().toISOString(),mode:'simulation'}));router.push('/portfolio')}return <main className="authShell"><div className="authVisual"><div className="authBrand"><span>AI</span><div><b>AI Company OS</b><small>PORTFOLIO OPERATING SYSTEM</small></div></div><div className="authOrb"><div><ShieldCheck size={28}/><b>YOUR COMPANIES</b><small>READY TO OPERATE</small></div></div><p>One operating system for intelligent companies, autonomous agents and measurable growth.</p></div><section className="authPanel"><div className="authCard"><div className="mobileBrand">AI COMPANY OS</div><div className="authEyebrow">SECURE ACCESS</div><h1>Welcome back.</h1><p className="authLead">Sign in to your portfolio command center.</p><form onSubmit={submit}><label>Email<div className="inputWrap"><Mail size={16}/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></div></label><label>Password<div className="inputWrap"><LockKeyhole size={16}/><input required minLength={4} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></div></label><div className="formRow"><label className="remember"><input type="checkbox"/> Remember me</label><Link href="/forgot-password">Forgot password?</Link></div>{error&&<div className="formError">{error}</div>}<button className="primaryAuth">Enter Portfolio <ArrowRight size={16}/></button></form><div className="authDivider"><span>or</span></div><button className="secondaryAuth" onClick={()=>{localStorage.setItem('aicos-session',JSON.stringify({mode:'simulation'}));router.push('/portfolio')}}>Continue in simulation mode</button><p className="authLead" style={{marginTop:18}}>New to AI Company OS? <Link href="/signup">Create an account</Link></p><small className="securityNote">Simulation credentials are local only. Real financial actions remain disabled.</small></div><Link className="backLink" href="/welcome">← Back to introduction</Link></section></main>}
