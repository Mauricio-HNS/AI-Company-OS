'use client';
import { FormEvent, useState } from 'react';
import { ArrowRight, LockKeyhole, Mail, ShieldCheck } from 'lucide-react';
import './auth.css';

export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');function submit(e:FormEvent){e.preventDefault();if(!email||!password){setError('Enter your email and password to continue.');return}window.location.href='/portfolio';}return <main className="authShell"><div className="authVisual"><div className="authBrand"><span>AI</span><div><b>AI Company OS</b><small>PORTFOLIO OPERATING SYSTEM</small></div></div><div className="authOrb"><div><ShieldCheck size={28}/><b>YOUR COMPANIES</b><small>READY TO OPERATE</small></div></div><p>One operating system for intelligent companies, autonomous agents and measurable growth.</p></div><section className="authPanel"><div className="authCard"><div className="mobileBrand">AI COMPANY OS</div><div className="authEyebrow">SECURE ACCESS</div><h1>Welcome back.</h1><p className="authLead">Sign in to your portfolio command center.</p><form onSubmit={submit}><label>Email<div className="inputWrap"><Mail size={16}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com"/></div></label><label>Password<div className="inputWrap"><LockKeyhole size={16}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></div></label><div className="formRow"><label className="remember"><input type="checkbox"/> Remember me</label><a href="/forgot-password">Forgot password?</a></div>{error&&<div className="formError">{error}</div>}<button className="primaryAuth">Enter Portfolio <ArrowRight size={16}/></button></form><div className="authDivider"><span>or</span></div><button className="secondaryAuth" onClick={()=>window.location.href='/portfolio'}>Continue in simulation mode</button><small className="securityNote">Protected by permission-gated execution. Real financial actions remain disabled.</small></div><a className="backLink" href="/welcome">← Back to introduction</a></section></main>}
