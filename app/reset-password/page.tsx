'use client';
import {FormEvent,useEffect,useState} from 'react';
import {ArrowRight,CheckCircle2,LockKeyhole} from 'lucide-react';
import Link from 'next/link';
import {useRouter,useSearchParams} from 'next/navigation';
import '../login/auth.css';
export default function ResetPassword(){const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[saved,setSaved]=useState(false);const[valid,setValid]=useState(false);const router=useRouter();const params=useSearchParams();useEffect(()=>{const token=params.get('token');setValid(!!token&&token===localStorage.getItem('aicos-recovery-token'))},[params]);function submit(e:FormEvent){e.preventDefault();if(valid&&password.length>=8&&password===confirm){const account=JSON.parse(localStorage.getItem('aicos-account')||'{}');localStorage.setItem('aicos-account',JSON.stringify({...account,passwordChangedAt:new Date().toISOString()}));localStorage.removeItem('aicos-recovery-token');setSaved(true)}}return <main className="authShell"><div className="authVisual"><div className="authBrand"><span>AI</span><div><b>AI Company OS</b><small>AUTONOMOUS COMPANY</small></div></div><div className="authOrb"><div>{saved?<CheckCircle2 size={30}/>:<LockKeyhole size={30}/>}<b>{saved?'ACCESS RESTORED':'NEW SECURITY KEY'}</b><small>ACCOUNT SECURITY</small></div></div><p>Your password protects access to the entire company portfolio and its operating controls.</p></div><section className="authPanel"><div className="authCard"><div className="mobileBrand">AI COMPANY OS</div>{saved?<><div className="authEyebrow">SECURITY UPDATED</div><h1>You are ready.</h1><p className="authLead">Your password has been updated successfully.</p><button className="primaryAuth" onClick={()=>router.push('/login')}>Continue to login <ArrowRight size={16}/></button></>:!valid?<><div className="authEyebrow">RECOVERY LINK INVALID</div><h1>Request a new link.</h1><p className="authLead">This recovery link is missing, expired or no longer valid.</p><Link className="primaryAuth" href="/forgot-password">Start recovery <ArrowRight size={16}/></Link></>:<><div className="authEyebrow">RESET PASSWORD</div><h1>Create a new password.</h1><p className="authLead">Use at least 8 characters and confirm the new password.</p><form onSubmit={submit}><label>New password<div className="inputWrap"><LockKeyhole size={16}/><input type="password" required minLength={8} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimum 8 characters"/></div></label><label>Confirm password<div className="inputWrap"><LockKeyhole size={16}/><input type="password" required value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repeat your password"/></div></label>{password&&confirm&&password!==confirm&&<div className="formError">Passwords do not match.</div>}<button className="primaryAuth" disabled={!password||password!==confirm}>Update password <ArrowRight size={16}/></button></form></>}</div></section></main>}
