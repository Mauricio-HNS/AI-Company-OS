/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/AI-Company-OS',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
